# Core does not read environment variables or require API keys.
# Vite loads frontend/.env.local when commands run from frontend/.
# Copy frontend/.env.example there; these values are shown for discoverability.
VITE_USE_MOCKS=true
VITE_API_BASE_URL=http://localhost:8000
