# Optional runtime advisor only. Offline mode does not read this file.
# Copy to .env only if it does not exist; keep the real key local.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# These are PROCESS environment settings, not read from this file:
# PowerShell: $env:FP_LLM_PROVIDER='openai'   (default: off)
# PowerShell: $env:PYTHONUTF8='1'
# PowerShell: $env:BACKEND_CORS_ORIGINS='http://localhost:5173,http://127.0.0.1:5173'
# Frontend settings belong in frontend/.env.local; see frontend/.env.example.
